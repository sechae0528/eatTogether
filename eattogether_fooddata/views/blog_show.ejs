<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/blog/list">블로그 리스트</a><br>
    <table width="400" border="1">
      <tr>
        <th width="30">제목</th>
        <td><%=doc.title%></td>
      </tr>
      <tr>
        <th>내용</th>
        <td><pre><%=doc.body%></pre></td>
      </tr>
      <tr>
        <th>날짜</th>
        <td><%=doc.date%></td>
      </tr>
    </table>
    <hr width="400" align="left">
    <table width="400" border="1">
      <% for(var i=0, arr=doc.comments, len=arr.length; i<len, b=arr[i]; i++) {%>
       <tr>
         <td><%=b.title%></td>
         <td><%=b.body%></td>
       </tr>
      <% } %>
    </table>
    <br>
    <form action="/blog/addComment" method="post">
      <input type="hidden" name="id" value="<%=doc._id%>">
      댓글 제목 : <input type="text" name="title"><br><br>
      댓글 내용 : <textarea name="body" rows="5" cols="30"></textarea><br>
      <input type="submit" value="댓글 쓰기">
    </form>
  </body>
</html>
